{% extends 'base.html.twig' %}

{% block title %}Bienvenue sur votre espace professionnel{% endblock %}

{% block body %}
<div class="container mt-5">
        <div class="row">
                <div class="col-12">
                        <h3 class="text-center">
                                Bonjour, <strong class="text-primary">{{ pro.nom | upper }}</strong> {{ pro.prenom | capitalize }}
                        </h3>
                </div>
        </div>
        <div class="row mt-4">
                <div class="col-12">
                        <a href="{{ path('app_planning') }}" class="btn btn-outline-primary btn-lg d-flex justify-content-center align-items-center" type="button">Mon planning <span class="material-symbols-outlined"><span class="material-symbols-outlined">calendar_month</span></span></a>
                </div>
        </div>
        <div class="row mt-4">
                <div class="col-12">
                        <a href="{{ path('app_seance') }}" class="btn btn-outline-primary btn-lg d-flex justify-content-center align-items-center" type="button">Mes séances <span class="material-symbols-outlined"><span class="material-symbols-outlined">book_online</span></span></a>
                </div>
        </div>



{% endblock %}