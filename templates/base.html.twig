<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="icon" href="{{ asset('img/logo_chu_bleu.png') }}">
    {{ encore_entry_link_tags('app') }}
    {% block stylesheets %}

    {% endblock %}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" rel="stylesheet" />
    <link href="{{ asset('styles/app.css') }}" rel="stylesheet">

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</head>
<body>
<nav class="navbar navbar-expand-lg bg-primary">
    <div class="container-fluid bg-primary">
        <a class="navbar-brand" href="{{ path('app_home') }}">
            <img src="{{ asset('img/logo_chu_blanc.png') }}" alt="CHU Sébastopol" height="70">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse bg-primary" id="navbarSupportedContent">
            <ul class="navbar-nav mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="#">Le CHU <span class="material-symbols-outlined">medical_services</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Informations pratiques <span class="material-symbols-outlined">info</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Nous rejoindre <span class="material-symbols-outlined">emoji_people</span></a>
                </li>
            </ul>

            <form class="d-flex" role="search" action="{{ path('app_index') }}">
                <input name="search" class="form-control me-2" type="search" placeholder="Professionnel" aria-label="Search">
                <button class="btn btn-success custom-btn" type="submit">Chercher</button>
            </form>



            {% if is_granted('ROLE_ADMIN') %}
                <span class="navbar-button d-flex gap-3 justify-content-center buttons">
                    <a href="{{ path('app_home') }}" class="btn btn-outline-secondary btn-lg d-flex justify-content-center align-items-center" type="button" id="connexion-button">Mon espace personnel<span class="material-symbols-outlined">account_circle</span></a>
                    <a href="{{ path('app_logout') }}" class="btn btn-outline-primary btn-lg d-flex justify-content-center align-items-center" type="button" id="connexion-button">Déconnexion <span class="material-symbols-outlined">logout</span></a>
                </span>
            {% elseif is_granted('ROLE_PRO') %}
                <span class="navbar-button d-flex gap-3 justify-content-center buttons">
                    <a href="{{ path('app_professionnel') }}" class="btn btn-outline-secondary btn-lg d-flex justify-content-center align-items-center" type="button" id="connexion-button">Mon espace personnel<span class="material-symbols-outlined">account_circle</span></a>
                    <a href="{{ path('app_logout') }}" class="btn btn-outline-primary btn-lg d-flex justify-content-center align-items-center" type="button" id="connexion-button">Déconnexion <span class="material-symbols-outlined">logout</span></a>
                </span>
            {% elseif is_granted('ROLE_USER') %}
                <span class="navbar-button d-flex gap-3 justify-content-center buttons">
                    <a href="{{ path('app_patient') }}" class="btn btn-outline-secondary btn-lg d-flex justify-content-center align-items-center" type="button" id="connexion-button">Mon espace personnel<span class="material-symbols-outlined">account_circle</span></a>
                    <a href="{{ path('app_logout') }}" class="btn btn-outline-primary btn-lg d-flex justify-content-center align-items-center" type="button" id="connexion-button">Déconnexion <span class="material-symbols-outlined">logout</span></a>
                </span>

            {% else %}
                <span class="navbar-button d-flex justify-content-center buttons">
                    <a href="{{ path('app_login') }}" class="btn btn-outline-primary btn-lg d-flex justify-content-center align-items-center" type="button" id="connexion-button">Connexion <span class="material-symbols-outlined">person</span></a>
                </span>
            {% endif %}

        </div>
    </div>
</nav>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
{% block body %}
{% endblock %}

</body>
</html>