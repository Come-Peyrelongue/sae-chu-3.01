{% extends '@EasyAdmin/page/content.html.twig' %}

{% block content %}
    <style>
        /* Styles pour la page d'accueil */
        .welcome-message {
            border: 1px solid var(--ea-border-color);
            border-radius: 10px;
            padding: 30px;
            max-width: 800px;
            margin: 50px auto;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            text-align: center;
            background-color: var(--ea-body-bg); /* Suivre la couleur du thème */
            color: var(--ea-text-color); /* Couleur du texte selon le thème */
        }

        .welcome-message h1 {
            font-size: 2.5em;
            margin-bottom: 20px;
        }

        .welcome-message p {
            font-size: 1.2em;
            line-height: 1.6;
            margin-bottom: 20px;
        }

        .welcome-message a.btn {
            text-decoration: none;
            background-color: #007bff;
            color: white;
            padding: 10px 20px;
            font-size: 1em;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        .welcome-message a.btn:hover {
            background-color: #0056b3;
        }

        /* Mode sombre (si géré par prefers-color-scheme) */
        @media (prefers-color-scheme: dark) {
            .welcome-message {
                box-shadow: 0 4px 6px rgba(255, 255, 255, 0.1);
            }

            .welcome-message a.btn {
                background-color: #375a7f;
            }

            .welcome-message a.btn:hover {
                background-color: #1d3557;
            }
        }
    </style>

    <div class="welcome-message">
        <h1>Bienvenue {{ admin.login }} dans la partie administration</h1>
        <p>
            Si vous voulez ajouter ou éditer des <strong>patients</strong> ou des <strong>professionnels</strong>,
            utilisez les options disponibles dans la barre de navigation à gauche.
        </p>
        <p>
            Pour consulter ou éditer des <strong>séances</strong>, cliquez sur ce lien :
            <a href="{{ path('admin') }}" class="btn btn-primary">Séances</a>
        </p>
    </div>
{% endblock %}
